<?php
/**
 * =================================================
 * Hook hnice_page
 * =================================================
 */
add_action('hnice_page', 'hnice_page_header', 10);
add_action('hnice_page', 'hnice_page_content', 20);

/**
 * =================================================
 * Hook hnice_single_post_top
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_single_post
 * =================================================
 */
add_action('hnice_single_post', 'hnice_post_thumbnail', 10);
add_action('hnice_single_post', 'hnice_post_header', 20);
add_action('hnice_single_post', 'hnice_post_content', 30);

/**
 * =================================================
 * Hook hnice_single_post_bottom
 * =================================================
 */
add_action('hnice_single_post_bottom', 'hnice_post_taxonomy', 5);
add_action('hnice_single_post_bottom', 'hnice_post_nav', 10);
add_action('hnice_single_post_bottom', 'hnice_display_comments', 20);

/**
 * =================================================
 * Hook hnice_loop_post
 * =================================================
 */
add_action('hnice_loop_post', 'hnice_post_header', 15);
add_action('hnice_loop_post', 'hnice_post_content', 30);

/**
 * =================================================
 * Hook hnice_footer
 * =================================================
 */
add_action('hnice_footer', 'hnice_footer_default', 20);

/**
 * =================================================
 * Hook hnice_after_footer
 * =================================================
 */

/**
 * =================================================
 * Hook wp_footer
 * =================================================
 */
add_action('wp_footer', 'hnice_template_account_dropdown', 1);
add_action('wp_footer', 'hnice_mobile_nav', 1);

/**
 * =================================================
 * Hook wp_head
 * =================================================
 */
add_action('wp_head', 'hnice_pingback_header', 1);

/**
 * =================================================
 * Hook hnice_before_header
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_before_content
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_content_top
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_post_content_before
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_post_content_after
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_sidebar
 * =================================================
 */
add_action('hnice_sidebar', 'hnice_get_sidebar', 10);

/**
 * =================================================
 * Hook hnice_loop_after
 * =================================================
 */
add_action('hnice_loop_after', 'hnice_paging_nav', 10);

/**
 * =================================================
 * Hook hnice_page_after
 * =================================================
 */
add_action('hnice_page_after', 'hnice_display_comments', 10);

/**
 * =================================================
 * Hook hnice_woocommerce_list_item_title
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_woocommerce_list_item_content
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_woocommerce_before_shop_loop_item
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_woocommerce_before_shop_loop_item_image
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_woocommerce_shop_loop_item_caption
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_woocommerce_after_shop_loop_item
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_product_list_start
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_product_list_image
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_product_list_content
 * =================================================
 */

/**
 * =================================================
 * Hook hnice_product_list_end
 * =================================================
 */
